### Phase 3. initramfs and root filesystem handoff

Purpose: storage and mount correctness.

* initramfs warnings/failures (missing modules, cryptsetup, mdraid, LVM)
* Root mount errors, fsck results, remount read-only events
* Swap activation issues