### Phase 1. Firmware and bootloader

Purpose: pre-kernel delays and platform anomalies.

* UEFI/BIOS events (if available), TPM events (if relevant)
* Bootloader timing and errors (systemd-boot/GRUB messages if surfaced)
* Secure Boot state (optional, security posture)