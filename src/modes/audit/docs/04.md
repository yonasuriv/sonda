### Phase 4. systemd userspace: critical path to graphical.target

Purpose: what actually delays “GUI up”.

* `systemd-analyze time` and `critical-chain`
* Failed units and “degraded” state
* Long-starting units (top 10)
* Dependency loops, timeouts, restart storms